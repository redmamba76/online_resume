<!DOCTYPE html >
<html lang="pt-br">
    <head>
        <title>Bruno de Moraes - Full Stack Developer</title>

        <meta charset="utf-8"/>

        <!-- Bootstrap -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
        <!-- JQuery -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
        <!-- Fontawesome -->
        <script src="https://use.fontawesome.com/c9a113e473.js"></script>

    </head>
    <body>
        <div class="container">
            <div class="row vertical-offset-80">
            <div class="col-md-4 col-md-offset-4">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="container" style="width: auto;">
                            <!-- 
                                inserir loggica para sucesso de cadastro e insucesso 
                            -->
                        </div>
                        <div class="row-fluid user-row">
                            <img src="" class="img-responsive" alt="O seu mini blog" />
                        </div>
                    </div><!-- fim panel heading -->
                    <div class="panel-body">
                        <form accept-charset="UTF-8" role="form" class="form-signin" action="/admin" method="post">
                            <fieldset>
                                <label>
                                    <div class="login_result"></div>
                                </label>
                                <input class="form-control" placeholder="Nome ou apelido" id="apelido" name="apelido" type="text" />
                                <br/>
                                <input class="form-control" placeholder="Senha" id="senha" name="senha" type="password" />
                                <br/>
                                <input class="btn btn-lg btn-success btn-block" type="submit" id="entrar" name="entrar" value="Entrar" />
                            </fieldset>
                        </form>
                    </div><!-- fim panel body -->
                </div>
                <div>
                    <% if(validacao.length > 0 ) { %>
                        <div class="row">
                            <div class="col-md-12">
                                <div class="alert alert-danger">
                                    <strong>Atenção!</strong>
                                    <ul>
                                        <% for(var i = 0, var n = validacao.length; i < n ; i++ ) { %>
                                            <li><%= validacao[i].msg %></li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    <% } // fim do if %>
                </div>
            </div>
        </div><!-- fim container global -->
    </body>
</html>